package me.alphamode.mcbig.extensions.client.gamemode;

import net.minecraft.client.Minecraft;
import net.minecraft.client.gamemode.GameMode;

import java.math.BigInteger;

public interface BigCreativeModeExtension {
    //? >=1.0.0-beta.8.0.r {
    /*static void creativeDestroyBlock(Minecraft minecraft, GameMode gameMode, BigInteger x, int y, BigInteger z, int face) {
        minecraft.level.extinguishFire(minecraft.player, x, y, z, face);
        gameMode.destroyBlock(x, y, z, face);
    }
    *///? }
}
